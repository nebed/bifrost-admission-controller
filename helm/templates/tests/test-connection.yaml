apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bifrost-admission-controller.fullname" . }}-test-connection"
  labels:
    {{- include "bifrost-admission-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bifrost-admission-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
